import rule

a = rule.Rule("Failed password", "Failed Login Attempt", "A login attempt was made on the server and was denied")
b = rule.Rule("testtesttest", "Test message", "test message successfully triggered")